{
  "name": "nodeapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "gypfile": "true",
  "scripts": {
    "test": "jest --coverage",
    "debug": "node --inspect app/index.js",
    "compile_commands": "npx node-gyp configure -- -f gyp.generator.compile_commands_json.py",
    "build:debug": "node-gyp --debug configure build && npm run compile_commands",
    "build": "node-gyp configure build && npm run compile_commands",
    "start:debug": "npm run build:debug && node app/index.js",
    "start": "node app/index.js",
    "clean": "node-gyp clean"
  },
  "author": "Andrea Bigagli",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "mongodb": "^3.6.3",
    "nocache": "^2.1.0"
  },
  "devDependencies": {
    "node-gyp": "^7.1.2",
    "node-addon-api": "^3.1.0"
  }
}
